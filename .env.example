# ═══════════════════════════════════════════════════════════
# Fyers Options Platform — Environment Variables
# ═══════════════════════════════════════════════════════════
#
# Copy this file to .env.local and fill in your values.
# NEVER commit .env.local to version control.

# ─── Fyers API Credentials ───
# Create an app at https://myapi.fyers.in/dashboard
FYERS_APP_ID=XXXXXXXXXX-100
FYERS_SECRET_KEY=your_secret_key_here

# ─── Fyers OAuth Redirect URI ───
# Must match the redirect URI configured in your Fyers app
FYERS_REDIRECT_URI=http://localhost:3000/api/auth/callback

# ─── Iron Session Password ───
# Must be at least 32 characters. Generate with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_PASSWORD=change_this_to_a_random_string_at_least_32_chars

# ─── App URL (for redirects) ───
NEXT_PUBLIC_APP_URL=http://localhost:3000
